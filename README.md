# Windows Active Directory with Linux BIND DNS — Complete Integration Guide
End-to-end implementation of Active Directory with external BIND DNS on AWS. Includes SRV record design, TSIG updates, _msdcs zone architecture, and domain join validation.
ReFer the HTML

🚀 Windows Active Directory + Linux BIND DNS on AWS

Complete production-ready guide to run Windows AD without Windows DNS, using Linux BIND 9 as authoritative DNS on AWS.

📌 Overview
Replace Windows DNS with BIND 9
Pre-create AD DNS records
Allow dynamic updates from Domain Controller
Run fully inside AWS VPC
🧱 Architecture
